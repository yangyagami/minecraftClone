CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(Minecraft)

SET(CMAKE_EXPORT_COMPILE_COMMANDS 1)

AUX_SOURCE_DIRECTORY(src SRC)
INCLUDE_DIRECTORIES(include)

ADD_EXECUTABLE(minecraft ${SRC})
TARGET_LINK_LIBRARIES(minecraft glfw X11)
